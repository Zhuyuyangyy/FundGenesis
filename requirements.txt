# FundGenesis - Requirements
# =============================================

# Core dependencies
numpy>=1.24.0
matplotlib>=3.7.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
scipy>=1.10.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
pytest-timeout>=2.1.0
httpx>=0.24.0

# Dev / Linting
ruff>=0.4.0
mypy>=1.5.0
pre-commit>=3.0.0

# Data & Serialization
orjson>=3.9.0

# Monitoring & Logging
rich>=13.0.0

# Standard library (no install needed): sys, json, typing, os, dataclasses, enum, asyncio, uuid, time, math, datetime, copy, threading, subprocess, multiprocessing, random, abc, collections
